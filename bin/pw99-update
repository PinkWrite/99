#!/usr/bin/env bash
# Pull origin/master and run SQL migrations. Keeps pw99-config.php.
#   bash bin/pw99-update
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec php "$ROOT/bin/update.php"
